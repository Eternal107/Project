<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Xamarin_JuniorProject.Controls"
             x:Class="Xamarin_JuniorProject.Views.AddPinPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{Binding ToolbarButtonText}"  Command="{Binding AddOrSave}"/>

    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout >
            <Entry Text="{Binding CurrentPin.Name}" Placeholder="Label" FontSize="20"/>
            <Entry Text="{Binding CurrentPin.Longtitude}" Placeholder="Longitude" IsEnabled="False" FontSize="20"/>
            <Entry Text="{Binding CurrentPin.Latitude}" Placeholder="Latitude" IsEnabled="False" FontSize="20"/>
            <Editor Text="{Binding CurrentPin.Description}" Placeholder="Description" AutoSize="TextChanges" MaxLength="100" FontSize="20"/>
            <StackLayout Orientation="Horizontal" >
                <Label Text="Favorite pin" FontSize="20"/>
                <CheckBox IsChecked="{Binding CurrentPin.IsFavorite}"/>
            </StackLayout>
            <controls:CustomMap PinSource="{Binding Pins}" MyMapClicked="{Binding MapClicked}" MapCameraPosition="{Binding MapCameraPosition}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>