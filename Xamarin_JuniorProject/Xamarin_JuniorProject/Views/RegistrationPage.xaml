<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:Xamarin_JuniorProject.Behaviors"
             xmlns:Resources="clr-namespace:Xamarin_JuniorProject.Resources"
             x:Class="Xamarin_JuniorProject.Views.RegistrationPage"
             Title="{Binding Title}"
             x:Name="this">
            
        
        <StackLayout VerticalOptions="Center">
        <StackLayout.Behaviors>
            <behaviors:ValidationGroupBehavior x:Name="form" IsValid="{Binding BindingContext.IsValid, Source={x:Reference this}, Mode=OneWayToSource}"/>
        </StackLayout.Behaviors>
        
        <Label Text="{x:Static Resources:AppResources.EnterYourLogin}"/>
        <Entry x:Name = "LoginEntry" ReturnType="Next" Placeholder = "{x:Static Resources:AppResources.Login}" Text="{Binding Login}">
            <Entry.Behaviors>
                <behaviors:ValidationBehavior Group="{x:Reference form}" 
                                              PropertyName="Text">
                    <behaviors:ValidationBehavior.Validators>
                        <behaviors:RequiredValidator />
                    </behaviors:ValidationBehavior.Validators>
                </behaviors:ValidationBehavior>
            </Entry.Behaviors>
        </Entry>

         <Label Text="{x:Static Resources:AppResources.EnterYourEmail}"/>
        <Entry x:Name = "emailEntry" Keyboard="Email" ReturnType="Next" Placeholder ="{x:Static Resources:AppResources.Email}" Text="{Binding Email}">
            <Entry.Behaviors>
                <behaviors:ValidationBehavior Group="{x:Reference form}" 
                                              PropertyName="Text">
                    <behaviors:ValidationBehavior.Validators>
                        <behaviors:RequiredValidator />
                        <behaviors:FormatValidator
                            Message="{x:Static Resources:AppResources.InvalidEmail}"
                            Format="^[_A-z0-9-]+(.[a-z0-9-]+)@[a-z0-9-]+(.[a-z0-9-]+)*(.[a-z]{2,4})$" />
                    </behaviors:ValidationBehavior.Validators>
                </behaviors:ValidationBehavior>
            </Entry.Behaviors>
        </Entry>
        
        <Label Text="{x:Static Resources:AppResources.EnterYourPassword}"/>
        <Entry x:Name = "passwordEntry" ReturnType="Next" Placeholder ="{x:Static Resources:AppResources.Password}" Text="{Binding Password}">
            <Entry.Behaviors>
                <behaviors:ValidationBehavior Group="{x:Reference form}" 
                                              PropertyName="Text">
                    <behaviors:ValidationBehavior.Validators>
                        <behaviors:RequiredValidator />
                        <behaviors:FormatValidator
                            Message="{x:Static Resources:AppResources.PasswordsMustBe10Digits}"
                            Format="^[0-9]{10}$" />
                    </behaviors:ValidationBehavior.Validators>
                </behaviors:ValidationBehavior>
            </Entry.Behaviors>
        </Entry>

        <Entry ReturnType="Done" Placeholder ="{x:Static Resources:AppResources.PasswordConfirmation}" >
            <Entry.Behaviors>
                <behaviors:ValidationBehavior Group="{x:Reference form}" 
                                              PropertyName="Text">
                    <behaviors:ValidationBehavior.Validators>
                        <behaviors:PasswordsEqualityValidator
                            Message="{x:Static Resources:AppResources.PasswordsDoNotMatch}"
                            confirmPassword="{x:Reference passwordEntry}"/>
                    </behaviors:ValidationBehavior.Validators>
                </behaviors:ValidationBehavior>
            </Entry.Behaviors>
        </Entry>

        <Button Text="{x:Static Resources:AppResources.Registration}" Command="{Binding ToTabbedPage}"
		        FontSize="Large"
		        HorizontalOptions="Center"
		        />
		    
        </StackLayout>
         
</ContentPage>